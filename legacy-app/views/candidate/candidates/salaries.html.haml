.content-i
  .content-box
    .row
      .col-sm-12
        .element-wrapper
          .element-box-tp
            .controls-above-table
              .row.mr-1.ml-1
                .col-sm-12
                  .form-inline.justify-content-sm-end
                    %input.form-control.form-control-sm.rounded.bright{placeholder: "Search", type: "text", type: 'search', id: "candidate_salaries_datatable-search"}
            .table-responsive
              %table.table.table-striped.table-bordered.table-hover.table-lg{id: "candidate_salaries_datatable"}
                %thead
                  %tr
                    %th CONTRACT
                    %th STATUS
                    %th START DATE
                    %th END DATE
                    %th BILLABLE AMOUNT
                    %th TOTAL PAID
                %tbody
                  -@salaries.each do |s|
                    %tr
                      %td= s.contract.project_name
                      %td= s.status
                      %td= s.contract_cycle.start_date.strftime("%d-%m-%Y")
                      %td= s.contract_cycle.end_date.strftime("%d-%m-%Y")
                      %td= s.total_amount
                      %td= s.billing_amount


- content_for :scripts do
  :javascript
    $(document).ready(function() {
      default_active_nav('.ln-7');
    });