%li
  = image_tag comment.try(:created_by).try(:photo) , class: 'online'
  %span.pull-right.font-xs.text-muted
    %i #{time_ago_in_words(comment.created_at.localtime)} ago
  %span.name= comment.try(:created_by).try(:full_name)
  = comment.try(:body)