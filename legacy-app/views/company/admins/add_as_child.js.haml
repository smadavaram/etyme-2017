- if flash[:errors].present?
  - flash[:errors].each do |e|
    flash_error("#{e}");
- if flash[:success].present?
  flash_success("#{flash[:success]}");

$("#team_members").html("#{ j render(partial: 'company/admins/partials/team_members', user: @user)}");
