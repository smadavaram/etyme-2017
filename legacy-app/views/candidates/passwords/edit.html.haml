= render 'partials/password_edit'

-content_for :scripts do
  :javascript
    $('#new-password-form').validate(
      {
         rules: {
          password:{
          required: true
          },
          cPassword:{
          required: true,
          equalTo: "#password"
          }
       }
     } );
