.ta-record.bg_white.pt-2.pl-2
  .ta-timestamp
    %strong #{a.try(:created_at).strftime('%I:%M %P')}
  .ta-activity
    #{a.owner.name.titleize} created job
    = link_to truncate(a.trackable.title), job_path(a.trackable), class: 'font-md'

-#%img.busy{:alt => "img", :src => "#{a.owner.try(:photo)}"}/
-#%span.name.font-sm.well.margin-bottom-10.text-center
-#  %span.text-muted
-#  %b
-#    #{link_to a.owner.full_name, a.owner if a.owner}
-#    %span.pull-right.text-muted
-#      %i= a.created_at.strftime('%H:%M:%S %-d %B %Y  ago')
-#    %p.font-md
-#      created job
-#      = link_to truncate(a.trackable.title), job_path(a.trackable), class: 'font-md'