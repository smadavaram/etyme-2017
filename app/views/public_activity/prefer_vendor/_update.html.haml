%img.busy{:alt => "img", :src => "#{a.owner.try(:photo)}"}/
%span.name.font-sm
  %span.text-muted
  %b
    #{link_to a.owner.full_name, a.owner if a.owner}
    %span.pull-right.font-xs.text-muted
      %i= time_ago_in_words(a.try(:created_at).try(:localtime))+" ago"
  %br/
  Updated
  = link_to a.trackable.company.name, profile_company_path(a.trackable.company), class: 'font-md'