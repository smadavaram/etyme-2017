.modal.fade.in{id: location.new_record? ? "new_location" : "location-#{location.id}"}
  .modal-dialog
    .modal-content
      .modal-header
        %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", :type => "button"}
          ×
        %h4#myModalLabel.modal-title
          %strong Add New Location
      = render 'company/locations/partials/form',location: location