-if flash[:errors].present?
  - flash[:errors].each do |e|
    flash_error("#{e}");
- if flash[:success].present?
  flash_success("#{flash[:success]}");
  $("#temp_candidates").append("<option value=' #{@candidate.id} ' selected> #{@candidate.full_name} </option>");
  $('#temp_candidates').trigger('change');
  $("#candidate-new-modal").modal('hide');
  new-company-contacts-modal