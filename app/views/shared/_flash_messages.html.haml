-#- if notice
-#  .alert.alert-success.fade.in
-#    %button.close{"data-dismiss" => "alert"}
-#      ×
-#    %i.fa-fw.fa.fa-check
-#    = notice
-#  - if alert
-#    .alert.alert-warning.fade.in
-#      %button.close{"data-dismiss" => "alert"}
-#        ×
-#      %i.fa-fw.fa.fa-warning
-#      = alert
:javascript
  function flash_alert(color , msg){
        $.smallBox({
          title : msg,
          //content : "<i class='fa fa-clock-o'></i> <i>2 seconds ago...</i>",
          color : color,
          iconSmall : "fa fa-thumbs-up bounce animated",
          timeout : 4000
        });
    //  $.amaran({
    //    'theme'     :'colorful',
    //    'content'   :{
    //        bgcolor: color,
    //        color:'#fff',
    //        message:msg
    //    },
    //    'cssanimationOut'   :'slideRight',
    //    'outEffect'         :'slideRight',
    //    'closeOnClick'  :true,
    //    'position'          :'top right',
    //    'closeButton': true
    //});
  }
- if flash[:alert].present? || flash[:toastr].present? || flash[:error].present? || flash[:errors].present? || flash[:success].present? || flash[:notice].present? || flash[:info].present?
  - if flash[:alert].present?
    :javascript
      flash_alert('#FFC333',"#{flash[:alert]}");
  - if flash[:error].present?
    :javascript
      flash_alert('#D04B2B',"#{flash[:error]}");
  - if flash[:errors].present?
    - flash[:errors].each do |e|
      :javascript
        flash_alert('#D04B2B',"#{e}");
  - if flash[:success].present?
    :javascript
      flash_alert('#739E73',"#{flash[:success]}");
  - if flash[:notice].present?
    :javascript
      flash_alert('#80C14B',"#{flash[:notice]}");
  - if flash[:info].present?
    :javascript
      flash_alert('#35A4DA',"#{flash[:info]}");

