.page-padding
  .all-wrapper.menu-side.with-pattern
    .auth-box-w
      .logo-w
        %a{:href => "/"}
          %img{:alt => "", :height => "54", :src => "#{image_path('etyme-logo-dark')}", :width => "image_width_for_height('logo/logo-big.png', 54)"}/
      %h4.auth-header Reset Password
      = bootstrap_form_for(resource, as: resource_name, url: password_path(resource_name), html: {method: :put, id: 'new-password-form', class: ' animated'}) do |f|
        = f.hidden_field :reset_password_token
        - flash.each do |key, value|
          = flash_message(key, value)
        .form-group
          = f.password_field :password, autofocus: true, autocomplete: "off", class: 'form-control', id: "password", required: true, minlength: 6, placeholder: "Enter Password"
          .help-block.form-text.with-errors.form-control-feedback
          .pre-icon.os-icon.os-icon-fingerprint
        .form-group
          = f.password_field :password_confirmation, autocomplete: "off", class: 'form-control', id: "cPassword", required: true, "data-rule-equalTo": "#password", minlength: 6, placeholder: "Confirm Password"
          .help-block.form-text.with-errors.form-control-feedback
          .pre-icon.os-icon.os-icon-fingerprint
        = f.submit 'Change my password', class: "btn btn-primary pull-right"
