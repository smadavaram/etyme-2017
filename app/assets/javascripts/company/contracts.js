function toogle_consultant(target,selector){
    if(target.value == "yes"){
        $(selector).show()
    }else{
        $(selector).hide()
    }
}

function toogle_seller(target,selector){
    if(target.value == "yes"){
        $(selector).show()
    }else{
        $(selector).hide()
    }
}